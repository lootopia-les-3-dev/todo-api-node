# Server
PORT=3000
NODE_ENV=development

# Database
DB_PATH=./todo.db

# API URL displayed in Swagger UI (set to your production URL in prod)
API_URL=http://localhost:3000

# CORS — set to your frontend origin in production (e.g. https://myapp.com)
ALLOWED_ORIGIN=*

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Sentry error monitoring DSN (leave empty to disable)
SENTRY_DSN=